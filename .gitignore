# =============================================================================
# macOS
# =============================================================================
.DS_Store

# =============================================================================
# Secrets & credentials
# =============================================================================
# All env files except the canonical example template
.env
.env.*
!.env.example
front/.env
mobile/.env

# Standalone credential files (never commit these — use AWS Secrets Manager)
aws-temp-creds
stripe_test_creds
podcastindex_creds
*_creds
# An ~/.aws/credentials copied into the repo, under any path. Its sibling
# ~/.aws/config IS tracked (infrastructure/aws/config.example) because it holds no
# secret, so the two names must be treated differently rather than by directory.
credentials
credentials.*
*.pem
*.key
*.p8
*.p12
*.jks
*.mobileprovision
media-summarizer-app_accessKeys.csv

# Auth scratchpads (test runs)
jwt_full.txt
magic_token.txt
last_email.txt
user_id.txt
user_record.json
verify_resp.json
billing_after_verify.json
minutes_initial.json
job_resp.json
pack_resp.json

# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.ruff_cache/
.uv-cache/
.coverage
coverage.xml
coverage.json
htmlcov/
.tox/
.nox/
.hypothesis/
.venv*/
venv/
env/
ENV/
env.bak/
venv.bak/

# Distribution / packaging
dist/
build/
*.egg-info/

# Runtime PIDs / sockets
*.pid
.uvicorn_api.pid

# Bootstrap shim that auto-loads .env.dev (legacy, do not commit)
sitecustomize.py

# Generated OpenAPI dump (canonical spec lives at docs/CANONICAL_MEDIA_API_OPENAPI.yaml)
openapi.json

# Misplaced pip arg files like `pip install =1.0.0`
=*.*.*

# =============================================================================
# Node / front / mobile
# =============================================================================
node_modules/
front/dist/
front/dist-ssr/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/ios/
mobile/android/
.metro-health-check*

# =============================================================================
# IDE / editor
# =============================================================================
.idea/
.vscode/
*.swp
*.swo
*~
.kiro/
.codex

# =============================================================================
# Logs
# =============================================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# =============================================================================
# Media files (raw audio/video should never be committed)
# =============================================================================
*.mp3
*.wav
*.mp4
*.m4a
*.ogg
*.flac
*.aac

# =============================================================================
# Docker
# =============================================================================
.docker/
docker-compose.override.yml

# =============================================================================
# Terraform
# =============================================================================
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
tfplan
tfplan-*
*.tfplan
# No .tfvars file is used anywhere since task-237 (one root module per
# environment, values as literals in envs/<env>/main.tf). The pattern stays as a
# guard: a .tfvars reappearing would be someone's local credentials, not config.
*.tfvars
.terraformrc
terraform.rc

# Lambda build artifacts produced at terraform apply time
infrastructure/terraform/.lambda-builds/
infrastructure/terraform/*.zip
infrastructure/terraform/modules/platform/.build/

# =============================================================================
# LocalStack
# =============================================================================
.localstack/
localstack-data/

# =============================================================================
# Claude / agent tooling
# =============================================================================
.claude/dispatch-runs/
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Captures d'écran et crash logs des beta testeurs, tirés d'App Store Connect
# par scripts/testflight_feedback.py. Jamais commités : le dépôt est public, et
# un crash log porte des chemins de conteneur et des identifiants d'incident.
# L'owner a déjà ces pièces dans TestFlight.
.testflight-feedback/

# =============================================================================
# Scratchpads / one-off planning docs at repo root
# =============================================================================
TASK_*.md
debug_*.py
missing_*.tsv
test_episode_order.py
test_matching_debug.py
test_search_episodes.py

# =============================================================================
# Temporary files
# =============================================================================
tmp/
temp/

# Maestro resumable-run markers and per-flow reports (written by CI)
.maestro-ios-passed
maestro-ios-reports/
.maestro-android-passed
maestro-android-reports/

# Firebase / FCM credentials — jamais dans le dépôt (public).
#
# `google-services.json` n'est PAS dans cette liste, et ce n'est pas un oubli :
# config client publique par conception, embarquée dans chaque APK, et surtout
# source de l'empreinte `runtimeVersion` — l'ignorer cassait tout build Android.
# Voir mobile/.gitignore et le commentaire `android` de mobile/app.config.ts.
#
# `google-services-key.json` reste listé alors que rien ne l'écrit plus : c'est le
# nom vers lequel `eas.json` pointait, et un téléchargement égaré à cet endroit ne
# doit pas devenir committable. `*-firebase-adminsdk-*.json` est le compte de
# service FCM v1 : clé privée RSA, se téléverse sur EAS, hors du dépôt.
google-services-key.json
*-firebase-adminsdk-*.json
