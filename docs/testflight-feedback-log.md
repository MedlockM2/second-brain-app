# Registre des feedbacks beta TestFlight

Un feedback par ligne, une fois qu'il est **tranché**. Ce fichier n'est pas un journal d'activité :
c'est la moitié durable du mécanisme de déduplication du triage quotidien.

## Comment il est lu

`.claude/agents/feedback-triage.md` considère qu'un feedback est **nouveau** si et seulement si son
`Feedback-Id` n'apparaît **ni** ici **ni** dans le message de commit d'une branche `feedback/*`.
Les deux ensembles se répartissent la mémoire :

| Où | Ce que ça veut dire |
|---|---|
| branche `feedback/*` vivante | proposition préparée, **en attente** de la décision de l'owner |
| ligne dans ce fichier | **tranché**, ne reviendra jamais |
| nulle part | nouveau, à traiter au prochain run |

La dédup est donc ancrée sur des **identifiants**, jamais sur la date du run précédent — la
convention posée par l'en-tête de `.github/workflows/mobile-build-watch.yml`, « parce que le
planificateur dérive et laisse tomber des runs ». Conséquence pratique : un matin manqué se rattrape
tout seul, et il n'existe aucun fichier d'état à réparer.

Un **no-go** s'inscrit ici comme un go. C'est ce qui l'empêche d'être reproposé le lendemain, puis
tous les jours suivants.

## Ce qu'on n'écrit pas ici

Le dépôt est public (`AGENTS.md`, « Never write secrets or account identity »). Donc :

- **jamais** l'e-mail ni le nom d'un testeur — les `Feedback-Id` d'Apple sont opaques et ne
  désignent personne ;
- **jamais** le `logText` brut d'un crash : il porte des chemins de conteneur et des identifiants
  d'incident. Seul le diagnostic qu'on en tire est écrit ;
- **jamais** de capture d'écran. Elles restent dans `.testflight-feedback/`, gitignoré, et l'owner
  les a déjà dans TestFlight.

## Issues possibles

| Issue | Sens |
|---|---|
| `merged` | go de l'owner, branche mergée sur `main` |
| `declined` | no-go de l'owner, branche supprimée — la raison est obligatoire |
| `no-action` | rien d'exploitable dans le feedback (vide, ou capture sans commentaire ni indice) |
| `backlog` | demandait un design ou un choix technique non tranché ; parti en tâche de backlog, dont l'id est en note |

## Feedbacks tranchés

| Feedback-Id | Date | Type | Build | Issue | Diagnostic / raison |
|---|---|---|---|---|---|
| `AEkNxirFwx5dX-XQa_sLdtM` | 2026-09-04 | bug | 4 | `merged` | Splash jamais masqué sur un cold start par partage : `preventAutoHideAsync()` au niveau module, seul `hideAsync()` dans la route `/`, que `+native-intent` court-circuite vers `/(tabs)/inbox`. `SplashGate` sous `AuthProvider` + `initAuth()` qui ne peut plus rester bloqué sur un `SecureStore` en échec. Le rapport de crash joint documentait un **second** défaut, sans rapport avec le splash et non couvert par ce correctif (erreur JS fatale sur un lancement sans interface, aucun garde-fou dans `mobile/`) → **task-355**. |
| `APfAZXNH8eTYJKcG1KxlZh4` | 2026-09-04 | ui | 6 | `merged` | Écart de section hérité par chaque rangée de tuiles (`marginBottom` du slot) + `minHeight: 112` et `justifyContent: flex-start`. Porté en `rowGap`/`marginBottom` sur la grille : ≈ 72 → ≈ 39 px entre rangées. Repro en Display Zoom (320 pt). |
| `AFmAenXhNBmN8_vbnUeTEts` | 2026-09-04 | ui | 6 | `merged` | Pas un padding manquant mais un débordement de 32 pt : encadré en `flex: 1`, enfants à hauteur intrinsèque, résumé dimensionné pour 390 pt. La carte mesure sa boîte et abaisse le plafond de lignes par puce (2 à 320 pt, 3 conservé à 390 pt). `Bullets.tsx` passe en `rowGap`, ce qui touche aussi l'écran Artefacts. |
| `AMJ0KSQjGg3YQ0EeAOegfk8` | 2026-09-04 | amélioration | 6 | `backlog` | Vignette d'un média partagé absente jusqu'à la fin de la transcription. Rien de local à afficher : `InboxItem` ne porte aucun URI de fichier, la tuile lit `image_url`/`media_image` du serveur. Owner : émission plus tôt côté backend plutôt que plombage de l'URI local, donc tâche unique sans benchmark → **task-353**. |
| `AE3J09ClZ0T9VY8Jc5SnCRc` | 2026-09-04 | ui | 6 | `declined` | Écarts inégaux entre les sections de l'onglet Accueil (≈ 27 pt contre ≈ 92 pt). Cause établie : `HomeTile` réservait la hauteur du texte le plus long (3 lignes + auteur) sous toutes les vignettes, donc une rangée de photos à légende d'une ligne laissait ≈ 62 pt de vide permanent juste avant la section suivante. **Raison du no-go (owner) : fausse alerte, on ne touche à rien** — l'écart ne justifie pas de changement, et le correctif proposé aurait échangé ce vide contre des bas de vignettes non alignés dans une rangée mixte. Branche `feedback/rythme-sections-accueil` supprimée. |
| `AFu3l0qVveQlqPBSLcWnn4U` | 2026-09-05 | bug | 6 | `declined` | Enregistrement d'un média Instagram en échec : bandeau « Le traitement a échoué » / « Unable to extract transcribable media from this Instagram URL. ». L'app rend fidèlement un état d'échec, le défaut est en amont dans l'ingestion. Trois sites d'émission de `instagram_ingestion_worker.py` (292 `apify_result_invalid`, 355 `resolver_non_retryable`, 500 `no_transcript_or_audio_url`) produisent ce même texte et ne sont discriminés que par `details`, qui part en logs — la cause n'était donc pas départageable depuis le feedback. **Raison du no-go (owner) : cas isolé sur un seul post, pas de signal de récurrence — on rouvrira si ça remonte plusieurs fois.** Aucun diagnostic n'a été lancé. L'observation secondaire tirée du même feedback — phrase anglaise servie à une interface `fr-FR` parce que le worker émet un message au lieu d'un code — est elle retenue et portée par **task-359**, qui ne couvre pas la cause racine de l'échec.
| `AAWwlEOYjzvk0z_3h7NdEQ4` | 2026-09-07 | amélioration | 6 | `merged` | Bouton partage de l'en-tête Média remplacé par un « … » Renommer / Supprimer, même bouton ajouté sur la page Collection. **Ce n'était pas un retrait de fonctionnalité** : le bouton n'avait aucun `onPress`, aucun handler ni dépendance de partage n'existe dans le dépôt — un bouton mort qui promettait ce que V1 n'a pas, et qui se rendait jusque dans les 7 rendus de l'en-tête (chargement, erreur, pending). Renommer et Supprimer existaient déjà de bout en bout (`AnchoredContextMenu`, `RenameDialog`, `useMediaActions`, `useCollectionActions`, endpoints en place), seulement atteignables en appui long depuis la Bibliothèque : du câblage, pas de la construction, donc **un seul chemin destructif** dans le code. Écarts assumés : « Déplacer » retiré du menu d'en-tête seul (`canMove`, l'icône dossier voisine ouvre déjà ce sélecteur) ; pas de menu sur la collection par défaut (le backend refuse `is_default`, deux lignes qui échoueraient) ni sur les états sans titre stocké. Les 11 catalogues i18n couverts. **Réserve non levée au merge** : la géométrie du menu n'avait jamais été exercée depuis une ancre en haut d'écran, calcul tenant en 320 pt mais non vérifié sur appareil — à regarder sur l'iPhone 13 mini d'où vient le retour. |
| `AHuuXfiAW78oUP7ZIA5V-ss` | 2026-09-10 | bug | 9 | `merged` | Aperçus de source bloqués sur « L'aperçu est en cours de rédaction… », un article restant sur le spinner le lendemain de son enregistrement. Contenu et statut sont **deux sources de vérité qui peuvent diverger par construction** (le contenu vient du mirroir `review_blurb` sur `user_media`, le statut d'une entrée d'artefact indexée sur le contenu), et côté mobile le spinner était l'**état par défaut** : la réconciliation testait `status === "failed"` et retombait sur `pending` pour tout le reste, donc un `ready` sans contenu attendait indéfiniment. `SourcePreview.tsx` : l'attente est désormais revendiquée, jamais supposée — seul un `pending` explicite fait tourner le spinner. `CompletedDetailView.tsx` : la réponse du `useFocusEffect` alimente enfin l'aperçu (sortir de l'écran et revenir devient le geste de reprise), la chaîne de `setTimeout` passe en `setInterval` possédé par l'effet (un instant non authentifié ne peut plus tuer le poll), le plafond de 20 lectures répond au lieu de s'arrêter en silence. Les 11 catalogues i18n : renommage de clé seulement (`preview.failed` → `preview.unavailable`), valeurs identiques à l'octet. **Le second symptôme décrit par le testeur n'en était pas un** : « Pas d'aperçu pour cette source. » est le comportement correct — l'owner a validé le groupement en connaissance de cause. **Question de design tranchée par l'owner** : la variante sobre est conservée, la ligne terminale dit la même chose dans les trois situations (échec réel, ligne incohérente, attente expirée), sans nouvelle clé ni bouton de reprise. Le correctif est un dernier rempart et ne fait pas apparaître les aperçus manquants : la cause amont côté `media_summarizer/` (blurb jamais déclenché sur une sauvegarde dédupliquée, `pending` sans borne d'âge) part en **task-391**. |
| `AFdL5Iv3FIQhnvMVGiggd-E` | 2026-09-10 | bug | 9 | `merged` | Partage d'une note Apple exportée en Markdown en échec sur « échec d'envoi, vérifiez votre connexion » — alors qu'aucun réseau n'était en jeu et que rien n'avait été envoyé. Un fichier local était lu avec `fetch(uri)` puis `.blob()`, ce qui n'est pas la façon de lire un fichier local. Lecture et envoi passent par un module partagé `mobile/src/services/localFileTransfer.ts` (`new File(uri)` pour normaliser l'URI, `uploadAsync` en flux natif pour ne pas faire passer 50 Mo par le tas JS), et chaque étape échoue avec son propre message. `bugReportService.uploadFileToS3` portait le même anti-pattern et reçoit le même correctif. Redaction des messages natifs durcie : un `NSError` de Cocoa cite le nom du fichier — donc le titre d'une note — sur un écran qui devient une capture dans App Store Connect. **Deux réserves assumées au merge** : le second point du testeur (message trop technique, à conformer à une décision récente) n'est traité qu'indirectement — la cause disparaît, mais le bloc « Détails techniques » est inchangé, même placement et même copie ; et la repro n'a pas pu être vérifiée faute d'appareil iOS avec Notes et la share extension, l'étape en échec et l'anti-pattern étant établis mais le déclencheur exact restant une hypothèse. |
| `AHEhMamt7UeqxkDIhIPATwE` | 2026-09-13 | amélioration | 9 | `backlog` | Temps d'attente d'un artefact sur un média fraîchement ajouté : la génération attend la traduction intégrale du transcript (60-90 s, task-203). Constat du testeur exact de bout en bout : `resolve_source` prend la clé traduite, `TranslationInProgressError` → source `pending(translation)`, rien n'est enfilé avant la fin de `transcript_translation_worker` ; et le travail est doublé, le prompt imposant déjà la langue cible via `corpus.language_instruction`. Le modèle voulu existe déjà pour l'aperçu de source (`review_blurb_service.py`, `reading_language=None`). Le triage proposait benchmark + implémentation ; **owner : pas de benchmark, le comportement cible est celui du testeur** → tâche unique **task-398** (lecture du transcript original, langue portée par le prompt, chemin d'attente de traduction supprimé, hypothèses de task-395 à relever). Libellé d'attente différencié (« Traduction en cours… ») : **écarté par l'owner**, aucune tâche. |
| `ADVR_i2r_aO1Y4BUsrNelo4` | 2026-09-10 | bug | 9 | `backlog` | Deux tuiles « ÉCHEC » sur un lien d'article web. Non diagnosticable depuis le feedback ; **investigué dans les logs CloudWatch dev à la demande de l'owner**. Cause : le site répond **HTTP 405** au User-Agent de l'extracteur (`media-summarizer/article-extractor`) et 200 à un navigateur — filtrage anti-robot, reproduit le 2026-09-16, toujours présent après task-392 (même User-Agent, 4xx rendu en `PROVIDER_UNAVAILABLE` non réessayable). Seconde tuile : le re-partage cinq minutes plus tard a réutilisé via l'idempotence le job déjà `failed`, sans relire la page — la ligne d'idempotence reste `failed` et condamne l'URL pour tous les comptes. → **task-399** (User-Agent navigateur, pas de réutilisation d'un job en échec, code juste pour les 4xx). Défaut vu sur la capture : titre de repli anglais « Article — 10 Sep 2026 », qui persiste aussi sur les médias réussis (12 photos importées sur dev) → **task-400**, owner : clé de libellé + date rendues par les catalogues i18n de l'app, pas de chaîne stockée. Raison d'échec sur la tuile de liste : **écartée par l'owner** — l'utilisateur doit seulement comprendre que c'est en échec, le badge suffit. |
| `AOMOaeiEMYPPFmT7wWYKSFs` | 2026-09-17 | bug | 9 | `backlog` | Vignette de l'Accueil figée sur l'effet de chargement une fois le traitement fini ; elle ne se met à jour qu'en ouvrant le média puis en revenant. Cause : la liste de l'Accueil n'est relue qu'au montage et au retour de focus de l'écran, rien ne relit l'état pendant qu'on y reste. Le triage proposait une relecture périodique bornée à 3 min suivie d'un message invitant à tirer pour actualiser ; **owner : refusé, l'arrêt et le message font porter une limite technique à l'utilisateur, reprise complète sur les pratiques UX établies** → benchmark non orienté **task-404**, implémentation **task-405**. |
| `AMRF4LBQiiNKUUzb6GjTU4U` | 2026-09-21 | ui | 9 | `merged` | Revue des non classés : gros vide sous l'aperçu, et « Jeter » qui ne réagit pas. **Deux défauts sans rapport, un seul écran.** Le vide : `styles.card` et `styles.blurbCard` en `flex: 1` dans une page que le pager étire sur toute la hauteur, donc l'encadré de résumé se tenait à la hauteur de l'écran quel que soit son contenu — à 896 pt, ≈ 280 pt de gris vide sous la dernière puce, **à l'intérieur d'un cadre dessiné**. Les deux surfaces passent en `flexShrink: 1` (carte ≈ 646 → ≈ 371 pt sur l'appareil du retour) ; le vide restant devient du fond plutôt qu'un cadre autour de rien. `fitBullets` a dû cesser de mesurer sa place sur cette boîte — une boîte qui épouse son contenu rapporte la hauteur de son propre contenu, donc la liste se serait comparée à elle-même et aurait rétréci ligne à ligne jusqu'au plancher — et budgète depuis la page, immobile car étirée par le pager : même nombre qu'avant, le plafond de 320 pt est inchangé. Un correctif antérieur (`a3fdca0`) n'avait lu que la moitié du problème : il bornait le *texte* à la boîte en laissant la boîte dimensionnée par l'écran. Le bouton : `handleDiscard` attendait le DELETE avant de retirer la page, et le drapeau « en vol » posait `disabled` sur un `Pressable` sans style désactivé — l'état pressé retiré sans rien mettre à la place, et le tap suivant avalé pendant toute la requête. La file avance désormais **au tap**, la requête suit, `restoreAt` réinsère à l'index en cas d'échec ; mutations via un `commitQueue` unique et handlers sur ref, pour que deux taps dans un même rendu ne retirent pas la carte voisine ; état pressé en `scale: 0.96`. iOS et Android, aucun `Platform.OS` ajouté. **Vérification sur appareil non faite au merge** (impossible depuis un worktree) : arrêt de la carte sous la dernière puce sur grand écran, carte suivante présente avant le réseau, padding bas des puces conservé à 320 pt. **Lien non tranché avec `AK1DS3kTae8nm6GpVzLveTg`** : ce feedback a été soumis 14 min après le « freeze », dont la capture montre le bandeau « Revue des non classés » à 20 — le `disabled` qui avalait les taps est un candidat sérieux au gel rapporté, mais le rapprochement n'est pas établi et fait l'objet d'une investigation logs demandée par l'owner. |
| `AFn1cs1fDboQ8EtjG6AXOes` | 2026-09-21 | bug | 9 | `merged` | Média partagé sur une session déconnectée : la vignette n'apparaît jamais sur l'Accueil, quel que soit le choix fait dans le modal de dossier. **Le rejeu n'était pas en cause** — l'intent est garé, l'utilisateur est envoyé se connecter, l'intake est soumis au retour, ce qui explique que le modal apparaisse. Le défaut est en aval : l'Accueil lit sa liste au montage et au focus, et ne la relit périodiquement que tant qu'une vignette *qu'il dessine déjà* est en traitement (`useProcessingRefresh`). Sur ce chemin la sauvegarde est créée strictement **après** la lecture de la liste (l'ingestion ne peut pas démarrer avant la connexion, les onglets n'existent qu'une fois authentifié, et les deux réponses à la question du dossier ferment le modal sans attendre la soumission — task-389) : la relecture part pendant que le POST est en vol, revient sans la sauvegarde, et plus rien ne la redemande. Un partage en session connectée gagne la même course parce que sa soumission démarre quelques secondes plus tôt. Correctif : `mediaSaveNotice`, avis au niveau module émis par `registerSave` et écouté par `useMediaList` et `useHomeSections`, qui font chacun une relecture silencieuse — **aucun planning, aucun intervalle, aucun polling ajouté** ; savoir quand sonder reste la question de `useProcessingRefresh`, que le statut non terminal de la vignette réarme. Seconde moitié : une réception est marquée `released` quand le modal la relâche, et une soumission qui échoue après coup est désormais annoncée par une alerte — un échec derrière un modal fermé ne laissait ni tuile ni raison, indiscernable d'une app qui aurait perdu le contenu. Couvre aussi la même course sur les gestes « + » et caméra. iOS et Android, JS partagé, aucune garde de plateforme. **Réserve assumée au merge** : on ignore si l'upload lui-même avait aussi échoué sur le run rapporté — il aurait fallu un appareil iOS avec la share extension ; l'alerte ajoutée est ce qui le nommera si ça se reproduit. |
| `AFQ4loQGwPhnLsdklF0WMRU` | 2026-09-21 | amélioration | 9 | `backlog` | Refonte UX de l'onglet Digest, demandée explicitement comme benchmark par le testeur : deux mouvements concurrents (carrousel horizontal, scroll vertical), « trop plein de bouton (ux fouilli) », et 4 directions de design attendues dans `mobile-design-mockups/`. Lecture de la capture, qui seule identifie l'onglet que le commentaire ne nomme pas : segment `Quotidien`/`Hebdomadaire`, titre « Votre journée en revue », compteur `1 / 7` et 7 points de pagination, **puis un second segment** `Lecture`/`IA`, puis « Aperçu » et « Texte complet » en scroll vertical — les deux axes décrits sont bien là, et **deux jeux de segments empilés avant d'atteindre le contenu** sont la source concrète du « trop plein de bouton ». Cible non connue et rien à coder avant arbitrage entre directions, donc découpage benchmark + implémentation (convention `CLAUDE.md`) → **task-408** (4 directions distinctes, chacune prenant parti sur la cohabitation des deux axes avec au moins une qui supprime un axe, design system *Amber Clarity* et tokens de `theme.ts` imposés, mesures à 414×896 pt et à 320 pt) et **task-409** (implémentation, `dependencies: [TASK-408]`, description générique qui défère au `Decision` du README — la recommandation du benchmark n'y est pas recopiée, l'owner pouvant en décider autrement). Le titre de repli « Photo — 18 sept. 2026 » et le badge `UNKNOWN` visibles sur la même capture **ne sont pas rouverts** : déjà tranchés en task-400, et hors du périmètre de la navigation de l'onglet. |
